package class16.me;

import java.util.LinkedList;
import java.util.Queue;

public class BFS {

    // 宽度优先遍历，随便选一个节点
    public <T> void BFS(Node<T> node) {
        if (node == null) return;
        Queue<Node<T>> queue = new LinkedList<>();

    }
}
